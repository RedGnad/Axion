/**
 * Axion entrypoint. The first milestone is the vertical slice:
 * one goal -> real on-chain hire(s) -> composed result, settled + verifiable on Base.
 * Run it with: `npm run slice` (loads .env, starts the leaf provider + the Axion buyer).
 */
console.log('Axion. Run the vertical slice with: npm run slice');
